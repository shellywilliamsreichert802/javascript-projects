// create an array variable containing the names
let namesInFamily = ["Michelle Renee", "Elijah Steven", "Zella Jolene"];

for (let i = 0; i < namesInFamily.length; i++) {
   console.log(namesInFamily[i]);
}
// write a for loop that prints each name on a different line
